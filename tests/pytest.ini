[pytest]
# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Paths
testpaths = tests

# Markers for test categorization
markers =
    unit: Fast, isolated unit tests with mocked dependencies
    integration: Integration tests with real models and database
    e2e: End-to-end pipeline tests

# Coverage configuration
addopts =
    --verbose
    --strict-markers
    --cov=geoparser
    --cov-report=html
    --cov-report=term-missing
    --cov-config=pyproject.toml

# Warnings
filterwarnings =
    error
    ignore::UserWarning
    ignore::DeprecationWarning

# Output
console_output_style = progress

# Coverage omissions (annotator excluded as per requirements)
# Additional configuration in pyproject.toml

